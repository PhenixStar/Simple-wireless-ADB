<?xml version="1.0" encoding="utf-8"?>
<!-- Trusted Device List Item -->
<com.google.android.material.card.MaterialCardView
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:layout_marginHorizontal="16dp"
  android:layout_marginVertical="4dp"
  app:cardCornerRadius="12dp"
  app:cardElevation="0dp"
  app:strokeWidth="1dp"
  app:strokeColor="?colorOutline"
  app:cardBackgroundColor="?colorSurfaceVariant">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:gravity="center_vertical">

    <!-- Device Icon -->
    <ImageView
      android:id="@+id/deviceIcon"
      android:layout_width="40dp"
      android:layout_height="40dp"
      android:src="@drawable/ic_device_phone"
      app:tint="?colorPrimary"
      android:background="@drawable/bg_icon_container_surface"
      android:padding="8dp"
      android:contentDescription="@string/trusted_devices" />

    <!-- Device Info -->
    <LinearLayout
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:layout_weight="1"
      android:layout_marginStart="12dp"
      android:orientation="vertical">

      <TextView
        android:id="@+id/deviceName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Device Name"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="?colorOnSurface"
        android:maxLines="1"
        android:ellipsize="end" />

      <TextView
        android:id="@+id/deviceIp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="192.168.1.100"
        android:textSize="12sp"
        android:fontFamily="monospace"
        android:textColor="?colorOnSurfaceVariant"
        android:layout_marginTop="2dp" />

      <TextView
        android:id="@+id/deviceAddedDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Added: Jan 15, 2025"
        android:textSize="11sp"
        android:textColor="?colorOnSurfaceVariant"
        android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- Remove Button -->
    <com.google.android.material.button.MaterialButton
      android:id="@+id/removeButton"
      style="@style/Widget.Material3.Button.IconButton.Outlined"
      android:layout_width="40dp"
      android:layout_height="40dp"
      app:icon="@drawable/ic_close"
      app:iconSize="18dp"
      app:iconTint="@color/error"
      android:contentDescription="@string/devices_remove" />

  </LinearLayout>

</com.google.android.material.card.MaterialCardView>
