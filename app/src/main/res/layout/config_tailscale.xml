<?xml version="1.0" encoding="utf-8"?>
<!-- Tailscale Mode Config -->
<LinearLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:orientation="vertical"
  android:padding="16dp">

  <!-- Tailscale Status -->
  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <ImageView
      android:id="@+id/tailscaleIndicator"
      android:layout_width="18dp"
      android:layout_height="18dp"
      android:src="@drawable/ic_indicator_vpn"
      app:tint="?colorOnSurfaceVariant" />

    <TextView
      android:id="@+id/tailscaleStatusText"
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:layout_weight="1"
      android:text="@string/tailscale_not_connected"
      android:textSize="14sp"
      android:textColor="?colorOnSurfaceVariant"
      android:layout_marginStart="12dp"
      android:accessibilityLiveRegion="polite" />

  </LinearLayout>

  <!-- Tailscale IP Display -->
  <TextView
    android:id="@+id/tailscaleIpText"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="---"
    android:textSize="24sp"
    android:textStyle="bold"
    android:fontFamily="monospace"
    android:layout_marginTop="8dp"
    android:textColor="?colorOnSurface"
    android:accessibilityLiveRegion="polite" />

  <!-- Relay Toggle -->
  <com.google.android.material.materialswitch.MaterialSwitch
    android:id="@+id/relaySwitch"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:layout_marginTop="12dp"
    android:text="@string/switch_relay"
    android:textSize="14sp"
    android:textColor="?colorOnSurface"
    android:contentDescription="@string/cd_relay_toggle"
    android:accessibilityLiveRegion="polite" />

  <!-- Port info -->
  <TextView
    android:id="@+id/portInfoText"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Port: 5555"
    android:textSize="12sp"
    android:textColor="?colorOnSurfaceVariant"
    android:layout_marginTop="4dp" />

</LinearLayout>
