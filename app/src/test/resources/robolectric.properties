# Robolectric configuration
# SDK 34 (Android 14) for testing
sdk=34

# Instrumented application class (optional - uses default if not specified)
# application=com.phenix.wirelessadb.TestApplication

# Instrument Conscrypt to avoid native JNI library loading issues
instrumentedPackages=org.conscrypt
